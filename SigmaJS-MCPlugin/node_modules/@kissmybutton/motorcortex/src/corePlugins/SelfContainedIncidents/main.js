const Clip = require('./SCGroup');
const Channel = require('./SCIChannel');
const ClipFromDefinition = require('./ClipFromDefinition');

module.exports = {
    npm_name: "@kissmybutton/self-contained-incidents",
    incidents: [
        {
            exportable: Clip,
            name: "Clip"
        }
    ],
    channel: Channel
};